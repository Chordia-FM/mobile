name: chordia_sync
description: >-
  The cross-device player mesh, ported wire-exact from the web client, plus the playback domain
  types the mesh and the player both speak. Pure Dart so it can be tested without a device.
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.16.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Sign-in handoff: the browser returns the one-time code here. Both flavours claim
                 the same scheme; with dev and prod side by side Android shows a chooser. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="chordia" android:host="auth"/>
            </intent-filter>
            <!-- App Links. Every link this product hands somebody is a page on the site, so a
                 shared album opens in the app rather than in a browser tab beside it. The paths are
                 the web client's own: `/app/...` covers every destination it has, and the short
                 forms beside it are what `shareUrl()` actually writes into a copied link (it drops
                 the `/app` prefix, and the site serves redirect stubs for these seven). They are
                 translated onto this client's tab-prefixed routes in `app/router.dart` —
                 registering a filter for a path the router cannot resolve would open the app onto
                 an error page, which is worse than leaving the link to the browser.

                 `autoVerify` needs a matching `/.well-known/assetlinks.json` on the site carrying
                 this app's signing certificate fingerprint; without it Android falls back to the
                 disambiguation chooser rather than opening the app directly. Both flavours claim
                 the same host, so with dev and prod side by side that chooser is expected. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="chordia.dev"/>
                <data android:scheme="https" android:host="www.chordia.dev"/>
                <data android:pathPrefix="/app"/>
                <data android:pathPrefix="/albums/"/>
                <data android:pathPrefix="/artists/"/>
                <data android:pathPrefix="/genres/"/>
                <data android:pathPrefix="/labels/"/>
                <data android:pathPrefix="/playlists/"/>
                <data android:pathPrefix="/smart/"/>
                <data android:pathPrefix="/tracks/"/>
                <data android:pathPrefix="/u/"/>
            </intent-filter>
        </activity>

        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Holds the process up while a batch of downloads finishes with the screen off. The work
             is in Dart; this only satisfies Android's requirement that long work be visible. -->
        <service
            android:name=".DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Opening the hub website for browser sign-in, billing and release notes. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
